apiVersion: v1
kind: Service
metadata:
  name: {{ include "searchworks-mcp.fullname" . }}
  labels:
    {{- include "searchworks-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "searchworks-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
