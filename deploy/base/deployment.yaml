apiVersion: apps/v1
kind: Deployment
metadata:
  name: searchworks-mcp
  labels:
    app.kubernetes.io/name: searchworks-mcp
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: searchworks-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: searchworks-mcp
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: searchworks-mcp
          # Replace with the immutable image produced by CI.
          image: ghcr.io/sul-dlss/searchworks-mcp:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: searchworks-mcp
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
